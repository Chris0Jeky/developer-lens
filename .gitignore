# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

# Developer Lens private runtime data. Never commit this directory.
.developer-lens/

# Local synthetic V2 bridge store. Invented C0 fixtures, rebuilt by npm run seed:v2.
.developer-lens-synthetic/

# Governor runtime cache — never authority. Losing it must not make the repo unresumable.
.agent-harness/runtime/

# Optional local environment configuration.
.env
.env.*
!.env.example
!.env.showcase

# Generated public showcase data. Always synthetic; rebuilt during deployment.
public/data/dashboard-*.json

# Test and coverage output.
coverage/

# Machine-local Claude trust settings. Never commit; bypassPermissions lives only here.
.claude/settings.local.json

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?
