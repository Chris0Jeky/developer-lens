# Developer Lens hosting reference

Reference-only preparation, 2026-09-10. This is not an executable prompt. The existing prompt library, continuation skill, owner constitution and data charter remain authoritative. `manifest.json` is inert hosting-intake metadata, not a new product schema or capability grant.

The public synthetic showcase stays static. The authenticated GitHub collector, local database and private analysis remain separate localhost work. A custom domain does not authorize hosted private analysis, new source collection, external model calls or telemetry. No runtime, data, DNS, name, account or workflow changes occur here. Existing main pushes may still publish after a future merge.

## Follow-on outcomes

DL1 prepares a canonical showcase origin with correct Vite base paths, refresh/deep links, social metadata and synthetic demo data. Keep the existing Pages address as a deliberate alias until the new site passes acceptance.

DL2 proves the public artifact boundary with the existing showcase/privacy checks. Never inspect or upload `.developer-lens/`, real datasets, credentials, browser profiles, private repository identifiers or generated private output as part of hosting preparation. Do not upload a repository root. Public source/documentation links are not the same as forbidden dataset-level repository URLs; retain that distinction in the existing privacy scan.

DL3 routes implementation through the existing continuation workflow and prompt library. No second governor, copied prompts or local harness is introduced. Any future product-name change must preserve stable capability/schema IDs, local-store paths and redaction behavior unless a separately approved migration is proven.

## Verification

Syntax: `python -m json.tool .hosting/manifest.json`. Applicable documentation checks are `npm run verify:context` and `git diff --check`. A future showcase slice uses `npm run build:showcase` and the relevant existing gate; JSON validation alone is not privacy, application or hosted acceptance.

Use synthetic fixtures only for this preparation work. Record exact-head checks and a previous-build rollback before any real publication. Unregistered domain/name candidates and private operational receipts remain outside the public repository.
