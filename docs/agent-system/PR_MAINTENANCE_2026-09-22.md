# PR maintenance continuation, 2026-09-22

This is an evidence and continuation record, not an owner decision or capability authorization. Live GitHub heads, reviews and checks take precedence over this snapshot. No protected data, provider credential, model, release or tag is activated by this work.

## Recovery and source reconciliation

The uploaded source archive identifies initial main `d67b3e5493b99a143a9b00a057e716888fdfc1bf`. The resumed GitHub main was `34a09f6697fe4247f458144caaedb3b7eb2c6595`: #350, #356, #347 and #352 had already merged. The continuation compared their ten changed paths with the archive rather than overwriting newer code. Local dependencies and the required Node 24 runtime were unavailable; a bounded npm bootstrap did not establish an installation. No local full-suite result is claimed. Dependency-free native probes are separate from the authoritative Node 24 PR gate.

## Published continuation work

| PR | Exact published head | Work and remaining proof |
| --- | --- | --- |
| #346 | `eaaac42630673e2b1ecd4231b998e75566c31c3f` | `agent_config` is a point-in-time per-repository boolean only, excluded from timing/history/portfolio aggregates; fresh review and exact-head gate required. |
| #349 | `6aef2c51da1926220a8a495dec91f893fc7e12d9` | All remaining flat-prefix descriptions reconciled to mandatory-role reservoirs and global fill; fresh review and exact-head gate required. |
| #353 | `3ec23fb1efefdb82d068339920c20638af1149e6` | Unresolvable lineage bound to its claim; missing scope distinguished from a cleared alias. Test-only run 35670366131 failed exactly five planted cases, with 1,566 passing and two skipped tests. Implementation gate and review required. |
| #351 | `7958066cea0674b245d0b78d9b9f6527fbc5c13a` | Windows candidate siblings and PATHEXT eligibility checked; files opened for ACL-aware readability without consuming content. Fourteen dependency-free local checks passed after ten failed against the exact prior blob. Hosted Vitest and integration proof remain required. |

#343 merged as an additive trusted-build-output check at `e91068fdc70e19d087607a1f4dd48a93acbde00c`, after exact-head run 35229884595/job 105230893037 passed and both review threads were resolved. It is not an arbitrary-markup browser sandbox. #362 tracks additional CSS-escape, URL-normalization and quoted-attribute coverage; branch `fix/showcase-resource-syntax-362` is its continuation.

## Explicitly unfinished lanes

- #328: invalidate stale persisted catalogue decisions and qualify the handoff reference as `Chris0Jeky/developer-lens::HUMAN_TODO.md::q-10(c)`; do not treat a previous progress message as published implementation.
- #344 and #345: inspect the review generated when each draft was marked ready; old green CI is not proof that new findings were resolved.
- #348: preserve or version the published ResearchFinding v1 contract before merging the disclaimer change.
- #354 and #357: reconcile the lazy-loading and manual-chunk alternatives; prove actual module evaluation rather than only asynchronous interaction assertions.
- #355: preserve production path confinement and obtain the affected Windows integration proof.
- #340: remains a component of #202's coherent pre-activation programme, not a standalone activation or #57 closure.
- #359 and #360: recovery dossiers, not wholesale merge candidates; map each retained invariant to current implementation or a named successor before closure.

## Tooling cleanup and guardrails

The temporary branch-only materializer generated immutable blobs from pinned public input hashes. Run 35670943628/job 106567016234 completed successfully and wrote no trees, commits, refs or merges. The connected Git-data actions separately published the inspected repairs. Its workflow file is removed in this maintenance revision and must not land on main.

Do not bypass missing branch-protection administration permissions. Use the declared `Prove the pull request` check, exact-head job logs, current review threads and SHA-guarded merge commits. Preserve the repository aging and post-merge sweep requirements. Record changed heads explicitly; neither queued checks nor old-head approvals establish readiness.

This record is intentionally a checkpoint. Subsequent maintenance commits or PR comments must record the final dispositions and any residual blockers before claiming completion.
